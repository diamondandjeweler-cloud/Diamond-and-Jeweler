# Audit Log — deferred / residual findings

Out-of-scope or partially-mitigated issues are logged here (AGENTS.md §4.4), not
fixed inline. One change, one concern.

| Date | ID | Residual | Reason deferred |
|------|----|----------|-----------------|
| 2026-07-11 | M1 | `restaurant.orders` anon INSERT (`rst_anon_insert_orders`) still lets an anonymous guest create an order against **any** tenant's branch. 0183 added a structural table_id↔branch_id consistency check, but full mitigation (an **active-branch** gate + per-source **rate limiting** + a **signed-QR token** binding the insert to the scanned table) needs live data verification and app-flow context that cannot be safely asserted in SQL without risking the guest-ordering flow. Order volume/tenant should be monitored until a signed-token QR issuance path exists. |

---

## 2026-07-12 — Wave A execution log

Deferrals and out-of-scope/un-verifiable items encountered during the **path-to-100 Wave A** execution
are recorded here by each batch (per AGENTS.md §4.4 — logged, not force-shipped). One row per deferral;
cite the item ID and the concrete reason.

| Date | Batch | Item | Residual / what was NOT done | Reason deferred |
|------|-------|------|------------------------------|-----------------|
| 2026-07-12 | 1 (DOCS) | — | No deferrals — all seven doc items (A5, A18, A16, A17, A13-doc, A20-doc, AUDIT_LOG header) completed. | — |
| 2026-07-12 | 2 (CI-GUARDS) | A21 (OG image) | OG image NOT shrunk to jpeg q82. `apps/web/public/og-image.png` (318 KB) is generated by `scripts/gen-og-image.mjs` and referenced as `.png` by `og:image` + `twitter:image` in `apps/web/index.html`. | Converting PNG→JPEG changes the public `og:image` URL and invalidates cached social-preview crawls (Facebook/Twitter/LinkedIn/WhatsApp) — an SEO/behavior change, not behavior-preserving. Needs owner/SEO sign-off + a coordinated cache-refresh, out of scope for an additive CI-guards batch. A lossless PNG re-compress (pngquant/oxipng) is the safe alternative but adds a heavy build dep; deferred. |
| 2026-07-12 | 2 (CI-GUARDS) | A21 (deno check) | `deno check` step landed BLOCKING but SCOPED: 6 of 48 edge entrypoints are grandfathered in a `KNOWN_UNTYPED` exclusion (`payment-webhook` 55 errs, `admin-refund` 13, `chat-support` 3, `chat-onboard` 3, `dsr-export` 2, `send-push-notification` 1 = 76 pre-existing type errors from loose Supabase query generics). | A blanket blocking `deno check supabase/functions/**/index.ts` is red today (76 errors), the same edge type-looseness `deno test --no-check` sidesteps. Excluding the 6 keeps the gate green with real teeth over the clean 42; burning the 6 down and removing them from `KNOWN_UNTYPED` is a separate typing task (tighten the query generics / add explicit row types). |
| 2026-07-12 | 2 (CI-GUARDS) | A9 | DB-types drift step added to db-apply job but ADVISORY (`continue-on-error: true`), not blocking. Could not be executed here (Docker down → no local Supabase). | `supabase gen types` output can shift with the CLI version; setup-cli pins `latest`, not the version the committed `db.generated.ts` was generated with, so a formatting-only delta could false-fail a BLOCKING money-path job. Promote to blocking once the CLI version is pinned. |
| 2026-07-12 | 2 (CI-GUARDS) | A11 | CI perf-budget job (size-limit, advisory) NOT added. | Requires installing `size-limit` + `@size-limit/preset-app` (new devDeps + config) and a bundle-size baseline; the install/baseline could not be verified green within the batch without risking the gate. Best-effort item explicitly deferrable per batch brief. Recommend a standalone advisory `perf-budget` job gating gzipped chunk sizes (entry ≤ ~90 KB, vendor-react ≤ ~260 KB) once baselines are captured. |
| 2026-07-12 | 2 (CI-GUARDS) | A12 | dependency-cruiser config + CI step (no-circular / domain-purity) NOT added. | Requires installing `dependency-cruiser` (new devDep) and authoring a `.dependency-cruiser.cjs` mirroring the eslint import-boundary rules; an unverified config risks a new red job. Deferred per best-effort clause. Recommend rules: `no-circular`, plus `app/**` may not import `views/**` internals, mirroring the existing eslint boundaries. |
| 2026-07-12 | 2 (CI-GUARDS) | A14 | Storybook axe CI step (`@storybook/test-runner`) NOT added. | Requires `@storybook/test-runner` + a Playwright browser install and a running Storybook build server in CI — a heavy, flaky addition that could bloat/break the gate. Deferred per best-effort clause. The existing `tests/e2e/a11y.spec.ts` (axe via `@axe-core/playwright`) already covers runtime a11y for the main routes; component-level Storybook axe can be layered later as its own non-blocking job. |
| 2026-07-12 | 4 (SAFE-REFACTORS) | A14 (tokenize) | Tokenized 5 app-surface `dark:*-gray-*` one-offs (ScreeningChecklist:107 hover-bg→`surface-2`, ColdStartPanel:167 divide→`border`, HRDashboard:81 hover-text→`fg`, BasicsSection:64 + TraitPicker:33 hover-border→`fg-subtle`). NOT tokenized: `TraitPicker:30` (`dark:bg-gray-200`/`dark:text-gray-900`/`dark:border-gray-200`); `sections.tsx:323,381` (`dark:border-gray-800`); `MatchApprovalPanel:266,372` + `ModerationPanel:294` (`dark:hover:bg-gray-600`); `EditRole:344` + `TraitPicker:32` (`dark:text-gray-600`); `AIChatPanel:342` + `SupportPanel:180` (`dark:bg-gray-900/40`). | Each deferred case lacks a parity-preserving token: **TraitPicker:30** is a deliberate INVERTED selected-chip (light chip on a dark surface) — no zinc surface token represents an inverted light fill. **sections.tsx** is the landing page's ADDITIVE navy/`midnight` marketing palette (`dark:bg-midnight-800`), not the zinc app tokens — a zinc `border` would clash with the navy section; belongs to the separate marketing-palette migration. **hover:bg-gray-600** is a hover background one step lighter than `surface-2`, and there is no `surface-3`/bg token for that step — mapping to `surface-2` would flatten the hover affordance (behaviour change). **text-gray-600** disabled/at-cap text has no exact-parity token (nearest `fg-subtle` #71717a is visibly lighter than gray-600 #4b5563). **bg-gray-900/40** needs an opacity modifier on a CSS-var token (`bg-canvas/40`), which Tailwind 3.4 does not reliably compose for `var()` colours lacking an `<alpha-value>` channel form. Unblock: add a `segmented`/inverted + channel-form alpha token set, and fold the marketing palette into its own token pass. |
| 2026-07-12 | 4 (SAFE-REFACTORS) | A7 | `useHmDashboardData.tsx` (787 lines) NOT decomposed into focused roles/candidates/interviews/points/reputation/company-link hooks. | The hook is a single-pass orchestration, not six separable slices: one `load()` effect writes EVERY slice in one ordered async sequence sharing mutable locals (`cancelled`, `watchdog`, `hmRoleIds`) + a single `writeDashCache`, and the realtime `matches` channel re-invokes the whole `load()` (with an unknown-role reload/resubscribe coalescing dance). Splitting into independent slice-hooks cannot preserve that exact ordering/timing/coalescing without a full re-architecture. Per §7.3 the only safety net is `useHmDashboardData.test.tsx` (4 cases: no-userId / empty / load / error) — it pins the `load()` macro-outcome but NOT the realtime path, the optimistic-update rollback ordering in `respond`/`doAction`, the 9 action handlers, or the watchdog. Extracting the action handlers would require prop-drilling ~15 setters/values and reproducing every `useCallback` dep array exactly (stale-closure risk) with zero test coverage. No safety net for those paths ⇒ defer (§7.3 "No safety net = do not refactor"). Unblock: first widen the characterization net to cover realtime add/update/delete + each action handler, THEN extract slice-by-slice against it. |
| 2026-07-12 | 4 (SAFE-REFACTORS) | A13-code | `RadioGroup` primitive NOT adopted on the `routes/onboarding/talent/DobStep.tsx` gender/race/commute single-selects (or `InterviewFeedback` 1–5). | The candidates render today as brand-highlighted **pill / segmented grid** `<button>`s (`bg-brand-500 text-white` selected). The `RadioGroup` primitive renders a vertical list of **radio-circle + label** rows (`RadioGroup.variants.ts` fixes an 18px circle control + label as grid siblings) — adopting it replaces the pill look with radio circles, an unrequested visual redesign on a pre-launch product with a designer owner. This violates BOTH the behaviour-preserving governor AND `docs/ui-adoption.md §4.1`'s own migration rule ("parity beats purity — **keep the same visible behaviour**"). The primitive has no `segmented`/`pill` variant, and its slot classes aren't per-item overridable, so parity isn't reachable via `className` passthrough alone. Unblock: add a `segmented` variant to `RadioGroup.variants.ts` (pill control, whole-row click target, brand-fill selected) so adoption keeps the current look while gaining Radix roving-focus + radio semantics — then wire the DobStep groups to it; or get owner sign-off on the radio-circle styling. |

